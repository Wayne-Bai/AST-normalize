(function( $, matrix ) {

	/*var staticLoaded = ["a.gif", "b.gif", "c.gif"];
	 var promises = [];
	 for (i = 0; i < staticLoaded.length; i++) {
	 (function(url, promise) {
	 var img = new Image();
	 img = new Image();
	 img.onload = function() {
	 promise.resolve();
	 };
	 img.src = url;
	 })(staticLoaded[i], promises[i] = $.Deferred());
	 }
	 $.when.apply($, promises).done(function() {
	 alert("All images ready sir!");
	 });*/

})( jQuery, matrix );
